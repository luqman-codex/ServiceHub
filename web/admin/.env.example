# REST API base — includes the /api/v1 version segment (02 §1.1)
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/api/v1
# localStorage key for the JWT (namespaced per app to avoid same-origin collision)
NEXT_PUBLIC_TOKEN_KEY=servicehub.admin.token
